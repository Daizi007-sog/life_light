'use client';

/**
 * 主页
 * TODO: 预留坑位 - 后续按设计稿更新
 */
export default function HomePage() {
  return (
    <div
      style={{
        minHeight: '100dvh',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 24,
        background: '#ffffff',
      }}
    >
      <p style={{ fontSize: 16, color: '#5B5C70' }}>主页（待设计）</p>
    </div>
  );
}
